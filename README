Basic REST API Implementation

Xiaokang Xin

This application is built from scratch using node.js, along with following node modules:
express
jade
mongoose
body-parser
request

Installation
Before install and run, make sure have npm and mongoDB installed.
in the app folder, run npm install to install all the dependecies.
run:
node app.js
test:
node test/spec.js
